/**
 * Base route for Webwow editor
 * URL: /webwow
 *
 * The WebwowBuilder component is now rendered in layout.tsx to persist
 * across route changes. This prevents remounts and duplicate API calls.
 *
 * Collaboration features (RealtimeCursors, ActivityNotifications) are
 * integrated in WebwowBuilderMain.tsx.
 */
export default function WebwowEditorRoute() {
  return null;
}
